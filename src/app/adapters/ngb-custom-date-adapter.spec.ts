import { NgbCustomDateAdapter } from './ngb-custom-date-adapter';

describe('NgbCustomDateAdapter', () => {
  it('should create an instance', () => {
    expect(new NgbCustomDateAdapter()).toBeTruthy();
  });
});
