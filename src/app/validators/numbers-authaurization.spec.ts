import { NumbersAuthaurization } from './numbers-authaurization';

describe('NumbersAuthaurization', () => {
  it('should create an instance', () => {
    expect(new NumbersAuthaurization()).toBeTruthy();
  });
});
