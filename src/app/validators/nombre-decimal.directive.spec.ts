import { NombreDecimalDirective } from './nombre-decimal.directive';

describe('NombreDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new NombreDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
