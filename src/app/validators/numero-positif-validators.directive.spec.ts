import { NumeroPositifValidatorsDirective } from './numero-positif-validators.directive';

describe('NumeroPositifValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new NumeroPositifValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
